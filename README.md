# LittleImageConverter
This Python tool applies a transparent overlay (Sample.png) to one or many images. It supports JPG, PNG, JPEG and WEBP, resizes each image to match the overlay, and saves results to a file or folder. Useful for batch editing, templates, and automated image processing.
